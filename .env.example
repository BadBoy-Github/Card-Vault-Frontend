# Frontend Environment Variables
# Copy this to .env and fill in your values

# API URL - use /api for Vercel production
# For local development, you can use http://localhost:5000/api
VITE_API_URL=/api

# Default admin email (this admin cannot be deleted or have role changed)
VITE_DEFAULT_ADMIN_EMAIL=your-admin-email@example.com
